// common.proto

syntax = "proto3";

package dgdo.common;

// Location without mutable timestamp for TripService
message Location {
  double lat = 1;
  double lon = 2;
}

// Metadata for telemetry / ML only
message Metadata {
  map<string, string> data = 1;
}
